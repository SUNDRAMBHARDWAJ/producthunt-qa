// Gitignored. The cf_clearance cookie is bound to IP + UA — do not commit it.
export const CLEARANCE_STATE = "playwright/.auth/clearance.json";
export const AUTH_STATE = "playwright/.auth/user.json";
